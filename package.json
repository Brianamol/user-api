{
  "name": "user-api",
  "version": "1.0.0",
  "description": "1. Clone the repository:\r    ```bash\r    git clone https://github.com/Brianamol/user-api.git\r    cd user-api\r    ```\r 2. Install dependencies:\r    ```bash\r    npm install\r    ```\r 3. Start the server:\r    ```bash\r    node index.js\r    ```\r 4. API will be available at `http://localhost:3000`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
